'use strict';

/**
 * total-count controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::total-count.total-count');
